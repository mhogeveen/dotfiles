theme = ""
visualizer = "ClassicPeak"
provider = "spotify"

sample_rate = 48000
buffer_ms = 500
resample_quality = 3
bit_depth = 32

[spotify]
client_id = "$CLIAMP_SPOTIFY_CLIENT_ID"
bitrate   = 320

[jellyfin]
url      = "$CLIAMP_JELLYFIN_URL"
token    = "$CLIAMP_JELLYFIN_TOKEN"
user_id  = "$CLIAMP_JELLYFIN_USER_ID"
